!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1, maximum-scale=1"}
    %title MIT Energy Career Fair 2016
    %link{:rel => "stylesheet", :href => "https://www.google.com/fonts#UsePlace:use/Collection:Source+Sans+Pro:400,300,600"}/
    = stylesheet_link_tag :application, :media => "all"
    = javascript_include_tag :application
    = csrf_meta_tags
  %body
    - if flash[:success]
      %script= "alert('#{flash[:success]}'); "
    %ul#nav
      %li{class: @current == 'Homepage' ? 'current' : ''}= link_to 'Homepage', root_path
      %li{class: @current == 'Students' ? 'current' : ''}= link_to 'Students', new_student_path
      %li{class: @current == 'Employers' ? 'current' : ''}= link_to 'Employers', '#'
    = yield
